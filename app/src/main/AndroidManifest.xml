<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.claresti.financeapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainDenarius"
            android:label="Denarius"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".Denarius"/>
        <activity android:name=".Registro" />
        <activity android:name=".Movimientos" />
        <activity android:name=".Configuracion" />
        <activity android:name=".AgregarCategoria" />
        <activity android:name=".AgregarCuenta" />
        <activity android:name=".Acerca" />
        <activity android:name=".DespliegueMovimientos" />
        <activity android:name=".Estadisticas"></activity>
    </application>

</manifest>